const deleteFav = require('./delete-favourite');
const productToFav = require('./product-to-fav');
const getFavs = require('./get-favs');

module.exports = {
  getFavs,
  deleteFav,
  productToFav,
};
